import '../style.css'
// Filled in during phase 7 (progress marker module).
document.getElementById('admin-root').innerHTML =
  '<p class="p-8 text-base-content">Admin panel — coming in phase 7.</p>'
